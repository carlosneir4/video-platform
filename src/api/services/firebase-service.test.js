import { firebaseService } from './firebase-service';

jest.mock('firebase');

describe("Check firebase service uoloadFile()", () => {

  test('should run callback with url video ', async () => {

  });
});